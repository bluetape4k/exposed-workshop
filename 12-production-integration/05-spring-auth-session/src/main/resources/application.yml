spring:
  application:
    name: spring-auth-session
  datasource:
    url: jdbc:h2:mem:spring_auth_session;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
